# Compatibility entrypoint.
# Install everything with:
#   pip install -r requirements.txt
#
# Or choose a narrower profile:
#   pip install -r requirements/crawl.txt
#   pip install -r requirements/ai.txt

-r requirements/all.txt
